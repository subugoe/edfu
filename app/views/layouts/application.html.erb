<!DOCTYPE html>
<html>
<head>
    <title>Edfu</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
</head>
<body>

<div id="main-container" class="container">
    <div class="row clearfix">
        <div class="col-md-12 column">
            <nav class="navbar navbar-default" role="navigation">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
                    </button>
                   <%= image_tag("logo.png", :alt => "Edfu logo", :class=> "navbar-brand") %>
                </div>

                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li class="active">
                            <%= link_to 'Import', uploads_path %>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Daten<strong class="caret"></strong></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <%= link_to 'Formular', formulare_path %>
                                </li>
                                <li>
                                    <%= link_to 'Götter', goetter_path %>
                                </li>
                                <li>
                                    <%= link_to 'Orte', orte_path %>
                                </li>
                                <li>
                                    <%= link_to 'Worte', worte_path %>
                                </li>
                            </ul>
                        </li>

                        <li>
                            <a href="#">Logbuch</a>
                        </li>
                        <li>
                            <a href="#">Handbuch</a>
                        </li>
                        <li>
                            <a href="#">Benutzer</a>
                        </li>
                        <li>
                            <% if user_signed_in? %>
                                <%="Hallo, #{current_user.email}"%> <%= link_to 'Sign out', destroy_user_session_path, :method => :delete %>
                            <% else %>
                                <%= link_to 'Sign in', new_user_session_path %>

                            <% end %>
                        </li>
                    </ul>
                </div>

            </nav>
            <%= yield %>
        </div>
    </div>
</div>

</body>
</html>