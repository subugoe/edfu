<!DOCTYPE html>
<html>
<head>
    <title>Edfu</title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
</head>
<body>

<div id="main-container" class="container">
    <div class="row clearfix">
        <div class="col-md-12 column">
            <nav class="navbar navbar-default" role="navigation">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                        <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
                    </button>
                    <%= link_to(image_tag("logo.png", :alt => "Edfu logo", :class => "navbar-brand"), uploads_path) %>
                </div>

                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                    <ul class="nav navbar-nav">
                        <li class="active">
                            <%= link_to 'Import', uploads_path %>
                        </li>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Daten<strong class="caret"></strong></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <%= link_to 'Formular', formulare_path %>
                                </li>
                                <li>
                                    <%= link_to 'Götter', goetter_path %>
                                </li>
                                <li>
                                    <%= link_to 'Orte', orte_path %>
                                </li>
                                <li>
                                    <%= link_to 'Worte', worte_path %>
                                </li>
                            </ul>
                        </li>

                        <li>
                            <a href="#">Szenen</a>
                        </li>
                    </ul>
                    <ul class="nav navbar-nav navbar-right">

                        <li>
                            <% if user_signed_in? %>
                                <%= link_to "#{current_user.email} abmelden", destroy_user_session_path, :method => :delete %>
                            <% else %>
                                <%= link_to 'Log in', new_user_session_path %>
                            <% end %>
                        </li>
                    </ul>
                </div>

            </nav>
            <%= yield %>
        </div>
    </div>
</div>

</body>
</html>