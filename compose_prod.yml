web:
  extends:
    file: compose_default.yml
    service: web
  links:
    - db:db
  environment:
    - solr=http://adw-pub.tc.sub.uni-goettingen.de/edfu/#/
    - DOCKER_ENV=production
    - RAILS_ENV=production

db:
  extends:
    file: compose_default.yml
    service: db
