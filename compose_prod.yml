web:
  extends:
    file: compose_default.yml
    service: web
  links:
    - db:db
  environment:
    - solr=http://adw-dev.tc.sub.uni-goettingen.de/solr/edfu
    - DOCKER_ENV=production
    - RAILS_ENV=production

db:
  extends:
    file: compose_default.yml
    service: db
